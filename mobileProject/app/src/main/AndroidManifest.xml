<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="e.dell.mobileproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnterPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pagecards" />
        <activity android:name=".DB_sqlite" />
        <activity android:name=".signup" />
        <activity android:name=".login" />
        <activity android:name=".StartingScreenActivity" />
        <activity android:name=".Question" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".QuizContract" />
        <activity android:name=".QuizDBHelper" />
        <activity android:name=".information" />
        <activity android:name=".GameOver"></activity>
    </application>

</manifest>